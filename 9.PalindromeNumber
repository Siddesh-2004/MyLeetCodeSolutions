class Solution {
public:
    bool isPalindrome(int x) {
        unsigned reverse=0,rem,temp=x;
        while(x>0){
            rem=x%10;
            reverse=reverse*10+rem;
            x/=10;
        }
        return reverse==temp;
    }
};
